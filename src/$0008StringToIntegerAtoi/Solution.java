package $0008StringToIntegerAtoi;

public class Solution {
    public int myAtoi(String s) {
        s = s.trim();
        int l = s.length();
        int flag = 1;
        if (s.charAt(0) == '+')
        for (int i = 0; i < l; i++) {
        }
        return 0;
    }
}
